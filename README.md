# Replace Texts in AutoCAD

Simple script that opens each DWG file in all subfolders of the parent folder, and replace all texts that match the given "old text" with the "new text" prompted by the user.
